{{ if eq .chezmoi.os "linux" -}}

{{ $packages := list
	"curl"
	"git"
	"vim"
	"zsh"
	"ripgrep"
	"fzf"
	"python3"
	"python3-pip"
	"tmux"
-}}

#!/bin/sh

sudo apt-get update
sudo apt-get install -y {{ $packages | join " " }}
{{ end -}}
